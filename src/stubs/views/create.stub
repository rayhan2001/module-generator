@extends('layouts.app')
@section('title', @$title)
@section('content')
    <div class="container-fluid">
        <form action="{{ route('{{pluralLower}}.store') }}" method="POST" enctype="multipart/form-data">
            @csrf
            @include('{{pluralLower}}.form')
            <button class="btn btn-primary">Save</button>
        </form>
    </div>
@endsection
