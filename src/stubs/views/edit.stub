@extends('layouts.app')

@section('content')
    <div class="container">
        <h1>Edit {{ Model }}</h1>
        <form action="{{ route('{{kebab_plural}}.update', $item->id) }}" method="POST">
            @csrf
            @method('PUT')
            @include('{{kebab_plural}}._form')
            <button class="btn btn-primary">Update</button>
        </form>
    </div>
@endsection
